apiVersion: v1
kind: Service
metadata:
  name: postgres-order-service
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres-order-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-order-service
  template:
    metadata:
      labels:
        app: postgres-order-service
    spec:
      containers:
        - name: postgres-order-service
          image: postgres-order-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: microservices-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: microservices-secret
                  key: DB_PASSWORD
            - name: DB_URL
              value: "jdbc:postgresql://postgres:5432/order_service_db"