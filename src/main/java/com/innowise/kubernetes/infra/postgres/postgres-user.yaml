apiVersion: v1
kind: Service
metadata:
  name: postgres-user-service
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres-user-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-user-service
  template:
    metadata:
      labels:
        app: postgres-user-service
    spec:
      containers:
        - name: postgres-user-service
          image: postgres-user-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: microservices-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: microservices-secret
                  key: DB_PASSWORD
            - name: DB_URL
              value: "jdbc:postgresql://postgres:5432/user_service_db"